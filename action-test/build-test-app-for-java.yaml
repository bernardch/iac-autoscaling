# # Everything done in the same environment
# name: Java CI with Gradle

# on:
#   push:
#     branches: [ master ]
#   pull_request:
#     branches: [ master ]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v2 # whenever we want to build, we first need to checkout our code - the actions/ path on GitHub is where reusable code is hosted.

#     - name: Set up JDK 1.8
#       uses: actions/setup-java@v1 # another predefined action; allows us to set up Java environment; 
#       with:
#         java-version: 1.8 # specifically Java 1.8

#     - name: Grant execute permission # Command Line Command
#       run: chmod +x gradlew

#     - name: Build with Gradle
#       run: ./gradlew build